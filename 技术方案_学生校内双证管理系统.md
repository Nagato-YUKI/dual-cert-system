# 学生校内双证管理系统 — 详细技术方案

---

## 一、需求理解与功能拆解

### 1.1 业务背景
"双证"指学生在校期间需要完成的**两类证书**考试：
- **人社证书**：人力资源和社会保障部门颁发的职业资格证书（如计算机等级、英语等级等）
- **专业证书**：专业相关的行业认证（如 Adobe 认证、华为认证、1+X 证书等）
- **校内引进证书**：学校引进的第三方证书考试

系统核心目标：**管理学生证书考试的全生命周期**，从报名、考试、审核、归档到统计分析。

### 1.2 功能模块拆解

| 编号 | 功能模块 | 对应需求 | 优先级 |
|------|---------|---------|--------|
| M1 | 证书类型与考试信息管理 | 需求1、3 | P0 |
| M2 | 学生报名管理 | 需求2 | P0 |
| M3 | 考试信息提交与审核（AI+人工） | 需求2 | P0 |
| M4 | 材料按班级归档 | 需求2 | P0 |
| M5 | 信息公开展示（培训、辅导材料） | 需求3 | P1 |
| M6 | 批量导入 | 需求4 | P1 |
| M7 | 教务系统对接接口 | 需求5 | P2（加分） |

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                    前端 (SPA)                      │
│        学生端             │       管理端           │
│   - 证书考试浏览          │  - 证书/考试管理       │
│   - 在线报名              │  - 报名审核（AI+人工）  │
│   - 成绩/证书查询         │  - 材料归档            │
│   - 培训材料查看          │  - 批量导入            │
│   - 个人中心              │  - 数据统计            │
└────────────┬────────────────────────────────────┘
             │ RESTful API (JSON)
┌────────────▼────────────────────────────────────┐
│              后端 API 服务 (Python)               │
│                                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐      │
│  │ 认证模块  │ │ 业务模块  │ │  AI 审核模块  │      │
│  │ JWT Auth │ │ CRUD API │ │  LLM 调用     │      │
│  └──────────┘ └──────────┘ └──────────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐      │
│  │ 导入导出  │ │ 归档模块  │ │  外部接口模块  │      │
│  │ Excel/CSV│ │ 文件管理  │ │  教务系统对接  │      │
│  └──────────┘ └──────────┘ └──────────────┘      │
└────────────┬────────────────────────────────────┘
             │
┌────────────▼────────────────────────────────────┐
│              数据层                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ SQLite / │  │ 文件存储  │  │ 缓存层    │       │
│  │ MySQL    │  │ 本地/OSS │  │ (可选)    │       │
│  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 理由 |
|------|------|------|
| 后端框架 | **Python Flask** | 轻量、灵活、适合快速原型；用户精通 Python |
| 数据库 ORM | **SQLAlchemy** | 成熟稳定，支持 SQLite（开发）和 MySQL（生产） |
| 数据库 | **SQLite（开发）→ MySQL（生产）** | SQLite 零配置起步，MySQL 适合生产环境 |
| 前端框架 | **原生 HTML/CSS/JS + Bootstrap 5** | 无需构建工具，快速开发；或使用 **Vue.js CDN** 提升交互 |
| AI 审核 | **通义千问 API / OpenAI API** | 国内可用、便宜、效果好的大模型 |
| 批量导入 | **openpyxl + pandas** | 处理 Excel/CSV 导入导出 |
| 文件上传 | **Flask-Uploads** | 材料归档的文件管理 |
| 认证 | **Flask-JWT-Extended** | 学生端/管理端身份认证 |
| 前端图表 | **ECharts CDN** | 数据统计可视化 |
| 部署 | **Gunicorn / Waitress** | Windows 环境下用 Waitress |

### 2.3 项目目录结构

```
dual-certificate-system/
├── app.py                    # 应用入口
├── config.py                 # 配置文件
├── requirements.txt          # Python 依赖
├── README.md                 # 项目说明
├── screenshots/              # 截图文件夹
├── docs/                     # 文档文件夹
│   ├── 需求文档.md
│   ├── 迭代记录.md
│   └── 路演PPT（待放）
├── app/
│   ├── __init__.py           # Flask 工厂函数
│   ├── models/               # 数据模型
│   │   ├── __init__.py
│   │   ├── student.py
│   │   ├── certificate.py
│   │   ├── exam.py
│   │   ├── registration.py
│   │   └── review.py
│   ├── routes/               # 路由/视图
│   │   ├── __init__.py
│   │   ├── auth.py           # 认证路由
│   │   ├── student.py        # 学生端路由
│   │   ├── admin.py          # 管理端路由
│   │   └── api.py            # 外部 API 路由
│   ├── services/             # 业务逻辑
│   │   ├── __init__.py
│   │   ├── ai_review.py      # AI 审核服务
│   │   ├── import_service.py # 批量导入服务
│   │   └── archive_service.py# 归档服务
│   ├── static/               # 前端静态资源
│   │   ├── css/
│   │   │   └── style.css
│   │   ├── js/
│   │   │   ├── student.js
│   │   │   └── admin.js
│   │   └── uploads/          # 上传文件存储
│   └── templates/            # 页面模板
│       ├── base.html
│       ├── student/
│       │   ├── dashboard.html
│       │   ├── certificates.html
│       │   ├── register.html
│       │   └── my_exams.html
│       └── admin/
│           ├── dashboard.html
│           ├── certificate_manage.html
│           ├── review.html
│           ├── import.html
│           ├── archive.html
│           └── statistics.html
└── data/                     # 数据文件
    └── dual_cert.db          # SQLite 数据库
```

---

## 三、数据库设计

### 3.1 ER 图（文字描述）

```
班级(Class) ──1:N──▶ 学生(Student)
学生(Student) ──1:N──▶ 报名记录(Registration)
证书类型(CertType) ──1:N──▶ 考试(Exam)
考试(Exam) ──1:N──▶ 报名记录(Registration)
报名记录(Registration) ──1:N──▶ 审核记录(ReviewLog)
证书类型(CertType) ──1:N──▶ 培训材料(TrainingMaterial)
证书类型(CertType) ──1:1──▶ 报名规则(RegistrationRule)
学生(Student) ──1:N──▶ 证书获取记录(CertificateRecord)
用户(User) ──1:N──▶ 审核记录(ReviewLog)
```

### 3.2 核心表结构

#### 班级表 (classes)
```sql
CREATE TABLE classes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100) NOT NULL,          -- 班级名称，如 "2024级软件技术1班"
    grade VARCHAR(20),                    -- 年级，如 "2024级"
    major VARCHAR(100),                   -- 专业
    department VARCHAR(100),              -- 院系
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 学生表 (students)
```sql
CREATE TABLE students (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_no VARCHAR(50) UNIQUE NOT NULL,  -- 学号
    name VARCHAR(50) NOT NULL,               -- 姓名
    class_id INTEGER REFERENCES classes(id), -- 所属班级
    phone VARCHAR(20),                       -- 手机号
    email VARCHAR(100),                      -- 邮箱
    id_card VARCHAR(18),                     -- 身份证号
    status VARCHAR(20) DEFAULT 'active',     -- 在校状态
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 证书类型表 (cert_types)
```sql
CREATE TABLE cert_types (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(200) NOT NULL,             -- 证书名称
    category VARCHAR(50) NOT NULL,          -- 分类：人社证书/专业证书/校内引进
    description TEXT,                        -- 证书描述
    issuing_authority VARCHAR(200),          -- 发证机构
    is_required BOOLEAN DEFAULT 0,          -- 是否必须完成
    is_recommended BOOLEAN DEFAULT 0,       -- 是否校内推荐
    status VARCHAR(20) DEFAULT 'active',    -- 状态
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 考试表 (exams)
```sql
CREATE TABLE exams (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    cert_type_id INTEGER REFERENCES cert_types(id),
    exam_name VARCHAR(200) NOT NULL,         -- 考试名称
    exam_date DATE,                           -- 考试日期
    registration_start DATE,                  -- 报名开始日期
    registration_end DATE,                    -- 报名截止日期
    exam_location VARCHAR(200),              -- 考试地点
    max_capacity INTEGER,                     -- 最大容量
    fee DECIMAL(10,2) DEFAULT 0,             -- 考试费用
    status VARCHAR(20) DEFAULT 'upcoming',   -- 状态：upcoming/ongoing/finished
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 报名记录表 (registrations)
```sql
CREATE TABLE registrations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id INTEGER REFERENCES students(id),
    exam_id INTEGER REFERENCES exams(id),
    status VARCHAR(30) DEFAULT 'pending',    -- pending/approved/rejected/paid
    submit_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    review_status VARCHAR(20) DEFAULT 'pending', -- pending/ai_reviewed/human_reviewed/approved/rejected
    ai_review_result TEXT,                    -- AI 审核结果 JSON
    ai_review_score DECIMAL(5,2),            -- AI 审核评分
    human_review_comment TEXT,               -- 人工审核意见
    reviewed_by INTEGER REFERENCES users(id),-- 审核人
    reviewed_at DATETIME,                     -- 审核时间
    materials_path VARCHAR(500),             -- 提交材料路径
    UNIQUE(student_id, exam_id)
);
```

#### 审核记录表 (review_logs)
```sql
CREATE TABLE review_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    registration_id INTEGER REFERENCES registrations(id),
    reviewer_id INTEGER REFERENCES users(id),
    review_type VARCHAR(20) NOT NULL,        -- ai_review / human_review
    result VARCHAR(20) NOT NULL,             -- approved / rejected / need_more_info
    comment TEXT,                             -- 审核意见
    score DECIMAL(5,2),                      -- 评分
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 证书获取记录表 (certificate_records)
```sql
CREATE TABLE certificate_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id INTEGER REFERENCES students(id),
    cert_type_id INTEGER REFERENCES cert_types(id),
    exam_id INTEGER REFERENCES exams(id),
    certificate_no VARCHAR(100),             -- 证书编号
    score DECIMAL(5,2),                      -- 考试成绩
    obtain_date DATE,                         -- 获取日期
    status VARCHAR(20) DEFAULT 'obtained',   -- obtained/pending/failed
    source VARCHAR(20) DEFAULT 'system',     -- 数据来源：system/manual_import/api
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 培训材料表 (training_materials)
```sql
CREATE TABLE training_materials (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    cert_type_id INTEGER REFERENCES cert_types(id),
    title VARCHAR(200) NOT NULL,
    content TEXT,                             -- 材料内容（富文本）
    file_path VARCHAR(500),                  -- 附件路径
    material_type VARCHAR(30) DEFAULT 'document', -- document/video/link
    is_public BOOLEAN DEFAULT 1,             -- 是否公开
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 报名规则表 (registration_rules)
```sql
CREATE TABLE registration_rules (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    cert_type_id INTEGER REFERENCES cert_types(id) UNIQUE,
    rule_content TEXT NOT NULL,               -- 规则内容（富文本）
    required_grades VARCHAR(200),            -- 适用年级
    required_majors TEXT,                    -- 适用专业（JSON数组）
    prerequisites TEXT,                      -- 前置条件
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 用户表 (users) — 管理端
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(200) NOT NULL,
    role VARCHAR(20) DEFAULT 'admin',        -- admin/super_admin
    name VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 四、API 接口设计

### 4.1 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 管理端登录 |
| POST | `/api/auth/student/login` | 学生端登录（学号+密码） |
| GET | `/api/auth/me` | 获取当前用户信息 |

### 4.2 证书管理接口（管理端）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/cert-types` | 证书类型列表 |
| POST | `/api/admin/cert-types` | 新增证书类型 |
| PUT | `/api/admin/cert-types/<id>` | 编辑证书类型 |
| DELETE | `/api/admin/cert-types/<id>` | 删除证书类型 |
| GET | `/api/admin/cert-types/<id>/rules` | 查看报名规则 |
| PUT | `/api/admin/cert-types/<id>/rules` | 编辑报名规则 |

### 4.3 考试管理接口（管理端）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/exams` | 考试列表（支持筛选） |
| POST | `/api/admin/exams` | 新增考试 |
| PUT | `/api/admin/exams/<id>` | 编辑考试 |
| DELETE | `/api/admin/exams/<id>` | 删除考试 |
| GET | `/api/admin/exams/<id>/registrations` | 查看报名情况 |

### 4.4 报名审核接口（管理端）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/reviews` | 待审核列表 |
| POST | `/api/admin/reviews/<id>/ai` | 触发 AI 审核 |
| POST | `/api/admin/reviews/<id>/batch-ai` | 批量 AI 审核 |
| PUT | `/api/admin/reviews/<id>` | 人工审核（通过/拒绝/要求补充） |

### 4.5 归档管理接口（管理端）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/archive/classes` | 按班级查看归档 |
| GET | `/api/admin/archive/class/<class_id>` | 某班级归档详情 |
| POST | `/api/admin/archive/export` | 导出归档数据 |
| GET | `/api/admin/archive/statistics` | 归档统计 |

### 4.6 批量导入接口（管理端）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/import/students` | 批量导入学生 |
| POST | `/api/admin/import/exam-results` | 批量导入考试成绩 |
| POST | `/api/admin/import/cert-records` | 批量导入证书获取记录 |
| GET | `/api/admin/import/template/<type>` | 下载导入模板 |

### 4.7 培训材料接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/materials` | 材料列表（管理端） |
| POST | `/api/admin/materials` | 新增/编辑材料 |
| GET | `/api/student/materials` | 材料列表（学生端公开） |
| GET | `/api/student/materials/<id>` | 材料详情 |

### 4.8 学生端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/student/exams` | 可报名考试列表 |
| POST | `/api/student/exams/<id>/register` | 报名考试 |
| GET | `/api/student/my-registrations` | 我的报名记录 |
| GET | `/api/student/my-certificates` | 我的证书获取记录 |
| GET | `/api/student/cert-types` | 证书类型浏览 |

### 4.9 外部 API 接口（加分项）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/external/cert-records` | 查询证书获取记录 |
| POST | `/api/external/cert-records` | 同步证书获取记录 |
| GET | `/api/external/students` | 查询学生信息 |
| POST | `/api/external/students/sync` | 同步学生数据 |

---

## 五、核心功能详细设计

### 5.1 AI 审核模块

**流程：**
```
学生提交报名 → 系统自动触发 AI 预审 → AI 返回审核结果 + 评分
  → 评分 ≥ 阈值 → 自动通过
  → 评分 < 阈值 → 进入人工审核队列
  → 人工审核 → 通过/拒绝/要求补充
```

**AI 审核检查项：**
- 学生基本信息是否完整（学号、姓名、班级）
- 是否满足报名条件（年级、专业要求）
- 提交材料是否齐全（检查文件数量、类型）
- 历史是否有重复报名
- 前置证书是否已获取（如高级证书需要初级证书）

**实现方式：**
```python
# ai_review.py 核心逻辑
def ai_review_registration(registration):
    prompt = f"""
    你是一个学生证书考试报名审核助手。请审核以下报名信息：
    - 学生：{student.name}，学号：{student.student_no}
    - 班级：{class.name}，专业：{class.major}
    - 报名考试：{exam.exam_name}（{cert_type.name}）
    - 报名条件：{rules}
    - 提交材料：{materials}
    
    请返回 JSON 格式：
    {{
        "result": "approved|rejected|need_more_info",
        "score": 0-100,
        "reason": "审核理由",
        "issues": ["问题列表"]
    }}
    """
    response = call_llm_api(prompt)
    return parse_response(response)
```

### 5.2 批量导入模块

**支持格式：** Excel (.xlsx) / CSV

**导入流程：**
```
上传文件 → 格式校验 → 预览数据 → 确认导入 → 逐行处理 → 返回结果
```

**三种导入类型：**

1. **学生信息导入**
   - 字段：学号、姓名、班级、手机号、邮箱、身份证号
   - 校验：学号唯一性、班级存在性

2. **考试成绩导入**
   - 字段：学号、证书类型、考试名称、成绩、考试日期
   - 校验：学生存在性、证书类型匹配

3. **证书获取记录导入**
   - 字段：学号、证书类型、证书编号、获取日期、成绩
   - 校验：学生存在性、证书编号唯一性

**实现方式：**
```python
# import_service.py 核心逻辑
def import_exam_results(file_path, import_type):
    df = pd.read_excel(file_path)  # 或 pd.read_csv
    results = {"success": [], "errors": []}
    
    for _, row in df.iterrows():
        try:
            validate_row(row, import_type)
            process_row(row, import_type)
            results["success"].append(row.to_dict())
        except ValidationError as e:
            results["errors"].append({"row": row.to_dict(), "error": str(e)})
    
    return results
```

### 5.3 材料归档模块

**归档逻辑：**
```
考试结束后 → 按班级分组 → 收集通过学生的材料 → 打包归档
```

**归档结构：**
```
archive/
├── 2024级/
│   ├── 软件技术1班/
│   │   ├── 计算机等级考试/
│   │   │   ├── 20230001_张三/
│   │   │   │   ├── 报名表.pdf
│   │   │   │   ├── 成绩单.pdf
│   │   │   │   └── 证书.pdf
│   │   │   └── 20230002_李四/
│   │   └── 1+X证书/
│   └── 软件技术2班/
└── 2025级/
```

**实现方式：**
```python
# archive_service.py 核心逻辑
def archive_by_class(exam_id):
    exam = Exam.query.get(exam_id)
    registrations = Registration.query.filter_by(
        exam_id=exam_id, 
        status='approved'
    ).all()
    
    # 按班级分组
    grouped = {}
    for reg in registrations:
        class_name = reg.student.class_.name
        if class_name not in grouped:
            grouped[class_name] = []
        grouped[class_name].append(reg)
    
    # 创建归档目录结构
    for class_name, regs in grouped.items():
        archive_dir = f"archive/{exam.exam_name}/{class_name}"
        os.makedirs(archive_dir, exist_ok=True)
        for reg in regs:
            copy_materials(reg, archive_dir)
    
    # 生成归档报告
    return generate_archive_report(grouped)
```

### 5.4 信息公开展示模块

**学生端展示内容：**
- 证书类型列表（分类展示：人社/专业/校内引进）
- 每种证书的详细介绍、发证机构
- 考试安排（时间、地点、费用）
- 报名规则（年级要求、专业要求、前置条件）
- 培训材料（文档、视频链接）
- 考试辅导材料下载

**管理端维护内容：**
- 证书信息 CRUD
- 报名规则编辑（支持富文本）
- 培训材料上传（支持 Markdown 编辑）
- 辅导材料管理（文件上传 + 分类）

---

## 六、前端页面设计

### 6.1 学生端页面

| 页面 | 路由 | 功能 |
|------|------|------|
| 首页/仪表盘 | `/student` | 证书考试概览、我的报名、证书获取统计 |
| 证书浏览 | `/student/certificates` | 按分类浏览所有证书类型 |
| 证书详情 | `/student/certificates/<id>` | 证书详情、报名规则、培训材料 |
| 考试报名 | `/student/exams` | 可报名考试列表 + 在线报名 |
| 我的报名 | `/student/my-registrations` | 报名记录、审核状态、结果 |
| 我的证书 | `/student/my-certificates` | 已获取证书列表 |
| 培训材料 | `/student/materials` | 公开培训材料查看 |

### 6.2 管理端页面

| 页面 | 路由 | 功能 |
|------|------|------|
| 仪表盘 | `/admin` | 数据概览、统计图表 |
| 证书管理 | `/admin/certificates` | 证书类型 CRUD |
| 考试管理 | `/admin/exams` | 考试安排 CRUD |
| 报名管理 | `/admin/registrations` | 查看所有报名 |
| 审核中心 | `/admin/reviews` | AI 审核 + 人工审核 |
| 批量导入 | `/admin/import` | 学生/成绩/证书批量导入 |
| 归档管理 | `/admin/archive` | 按班级查看/导出归档 |
| 材料管理 | `/admin/materials` | 培训/辅导材料管理 |
| 规则管理 | `/admin/rules` | 报名规则编辑 |
| 数据统计 | `/admin/statistics` | 各维度统计图表 |

### 6.3 仪表盘统计指标

- 证书类型总数（按分类）
- 考试场次统计
- 报名人数趋势
- 审核通过率
- 各班级证书获取率
- 各证书类型通过率

---

## 七、各功能难度评估

| 模块 | 功能 | 难度 | 说明 |
|------|------|------|------|
| **M1 证书/考试管理** | 证书类型 CRUD | ⭐ | 标准 CRUD，无技术难点 |
| | 考试安排 CRUD | ⭐ | 同上 |
| | 报名规则管理 | ⭐⭐ | 需要富文本编辑器集成 |
| **M2 学生报名** | 学生端浏览/报名 | ⭐⭐ | 需要前后端联调、状态管理 |
| | 报名条件校验 | ⭐⭐ | 需要灵活的规则引擎 |
| **M3 AI 审核** | AI 预审 | ⭐⭐⭐ | 需要 LLM API 对接、Prompt 工程 |
| | 人工审核 | ⭐⭐ | 标准审核流程 |
| | 批量 AI 审核 | ⭐⭐⭐ | 需要处理并发、限流 |
| **M4 材料归档** | 按班级归档 | ⭐⭐ | 文件操作 + 目录结构管理 |
| | 归档导出 | ⭐⭐ | 打包下载 |
| **M5 信息展示** | 培训材料管理 | ⭐⭐ | 文件上传 + 富文本 |
| | 公开展示 | ⭐ | 列表 + 详情页 |
| **M6 批量导入** | Excel/CSV 导入 | ⭐⭐ | 需要 openpyxl/pandas |
| | 数据校验 | ⭐⭐⭐ | 错误处理、部分成功逻辑 |
| | 模板下载 | ⭐ | 简单的文件下载 |
| **M7 教务对接** | 外部 API 接口 | ⭐⭐⭐ | 需要设计 API 规范、鉴权 |
| | 数据同步 | ⭐⭐⭐⭐ | 需要处理冲突、增量同步 |
| **基础架构** | 用户认证 | ⭐⭐ | JWT 实现 |
| | 权限控制 | ⭐⭐ | 学生/管理员角色 |
| | 前端框架搭建 | ⭐⭐ | 页面布局 + 路由 |
| | 数据库设计 | ⭐⭐ | 需要仔细设计表关系 |

---

## 八、可拓展方向分析

### 8.1 接入外部 API 增强功能

| 拓展方向 | 接入 API | 功能增强 | 难度 | 价值 |
|----------|---------|---------|------|------|
| **OCR 识别** | 百度 OCR / 通义千问 OCR | 自动识别证书照片、成绩单，自动提取信息 | ⭐⭐⭐ | 高 |
| **人脸识别** | 百度 AI / 腾讯 AI | 考试签到、身份核验 | ⭐⭐⭐ | 中 |
| **短信通知** | 阿里云短信 / 腾讯云短信 | 报名成功通知、审核结果通知、考试提醒 | ⭐⭐ | 高 |
| **邮件通知** | SMTP / SendCloud | 同上，邮件版 | ⭐ | 中 |
| **微信推送** | 微信公众号/企业微信 API | 通过微信推送通知 | ⭐⭐⭐ | 高 |
| **钉钉集成** | 钉钉开放平台 API | 审批流集成、通知推送 | ⭐⭐⭐ | 中 |
| **支付对接** | 微信支付 / 支付宝 | 在线缴纳考试费用 | ⭐⭐⭐⭐ | 中 |
| **电子签章** | e签宝 / 法大大 | 证书电子签章、防伪 | ⭐⭐⭐ | 中 |
| **云存储** | 阿里云 OSS / 腾讯云 COS | 材料云端存储、CDN 加速 | ⭐⭐ | 高 |
| **数据大屏** | ECharts / DataV | 实时数据大屏展示 | ⭐⭐ | 中 |

### 8.2 功能拓展方向

| 拓展方向 | 说明 | 难度 | 价值 |
|----------|------|------|------|
| **证书推荐系统** | 根据学生专业、年级推荐适合的证书 | ⭐⭐⭐ | 高 |
| **学习进度追踪** | 追踪学生备考进度、刷题记录 | ⭐⭐⭐ | 中 |
| **在线模拟考试** | 集成在线题库，模拟考试功能 | ⭐⭐⭐⭐ | 中 |
| **证书到期提醒** | 部分证书有效期管理，到期自动提醒 | ⭐⭐ | 中 |
| **多角色权限** | 班主任、辅导员、院系管理员等多角色 | ⭐⭐⭐ | 中 |
| **移动端适配** | 响应式设计或小程序版本 | ⭐⭐⭐ | 高 |
| **数据导出** | 导出 Word/PDF 格式的报表 | ⭐⭐ | 中 |
| **日志审计** | 操作日志记录、审计追踪 | ⭐⭐ | 中 |
| **国际化** | 多语言支持（中英文） | ⭐⭐ | 低 |

### 8.3 教务系统对接详解（加分项）

教务系统对接的核心挑战和方案：

```
┌──────────────┐         ┌──────────────────┐
│  双证管理系统  │  ◄──►  │  教务系统 API      │
│              │  REST   │  (假设存在)        │
└──────────────┘         └──────────────────┘

对接内容：
1. 学生基本信息同步（学号、姓名、班级、专业）
2. 学生学籍状态同步（在校/毕业/休学）
3. 证书获取情况同步（写入教务系统）
4. 成绩数据同步（从教务系统获取已有成绩）
```

**对接方案设计：**
```python
# 教务系统适配器模式
class EduSystemAdapter:
    """教务系统对接适配器基类"""
    def sync_students(self): pass
    def sync_cert_records(self): pass
    def get_student_info(self, student_no): pass
    def update_cert_status(self, student_no, cert_data): pass

# 具体实现（根据实际教务系统 API 调整）
class FZRJEduSystem(EduSystemAdapter):
    """福软教务系统对接"""
    BASE_URL = "https://edu-api.example.com"
    API_KEY = os.getenv("EDU_API_KEY")
    
    def sync_students(self):
        """从教务系统同步学生数据"""
        ...
    
    def sync_cert_records(self):
        """向教务系统推送证书获取记录"""
        ...
```

---

## 九、开发迭代计划（建议）

### 第1轮迭代（3-4天）：基础框架 + 核心 CRUD
- 项目脚手架搭建
- 数据库模型创建
- 证书类型、考试管理 CRUD
- 基础页面框架

### 第2轮迭代（3-4天）：学生报名 + 审核流程
- 学生端报名功能
- 管理端审核功能
- AI 审核集成
- 报名状态管理

### 第3轮迭代（2-3天）：归档 + 批量导入
- 材料归档功能
- 批量导入（学生/成绩/证书）
- 导入模板下载

### 第4轮迭代（2-3天）：信息公开 + 统计
- 培训材料管理
- 辅导材料展示
- 数据统计仪表盘
- 图表可视化

### 第5轮迭代（1-2天）：加分项 + 打磨
- 教务系统对接接口
- 通知功能
- 界面优化
- 测试与修复

---

## 十、关键技术要点提醒

### 10.1 AI 审核实现要点
- Token 消耗控制：批量审核时控制并发，避免 API 费用过高
- 降级策略：AI 不可用时自动降级为纯人工审核
- Prompt 版本管理：不同证书类型可使用不同审核 Prompt
- 审核结果缓存：相同条件不重复调用 AI

### 10.2 批量导入实现要点
- 大文件处理：分片上传 + 异步处理
- 错误容忍：部分行失败不影响其他行
- 结果反馈：详细展示成功/失败行及原因
- 回滚机制：支持撤销上次导入

### 10.3 安全注意事项
- 密码加密存储（bcrypt）
- JWT Token 过期管理
- 文件上传类型/大小限制
- SQL 注入防护（ORM 层已处理）
- XSS 防护（模板自动转义）

---

## 十一、推荐启动命令

```bash
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate

# 安装依赖
pip install flask flask-sqlalchemy flask-jwt-extended flask-cors
pip install openpyxl pandas
pip install openai  # 或 dashscope（通义千问）
pip install waitress python-dotenv

# 初始化数据库
python -c "from app import db; db.create_all()"

# 启动开发服务器
python app.py
# 或生产环境
waitress-serve --port=5000 app:app
```